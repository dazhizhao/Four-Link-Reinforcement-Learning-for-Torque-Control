name: bridge-robot-cloud
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - numpy
  - scipy
  - matplotlib
  - imageio
  - pandas
  - pyyaml
  - jupyterlab
  - ipykernel
  - tqdm
  - pytest
  - pip:
      - gymnasium>=0.29,<1.1
      - imageio-ffmpeg>=0.6,<0.7
      - stable-baselines3>=2.3,<3.0
